// Unit tests for refreshToken.controller.ts
import { refreshToken } from '../../../../src/modules/auth/controllers/refreshToken.controller';

describe('Refresh Token Controller', () => {
  it('should refresh the token successfully', () => {
    // Add test logic here
  });
});
